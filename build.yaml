---
- hosts: localhost
  become: false
  collections:
    - containers.podman
  tasks:
  - name: run the build in a kubernetes pod
    containers.podman.podman_play:
      kube_file: podman_os_play.yaml
      state: started